import { defineStore } from "pinia";

const useCityStore = defineStore("city", {
    // 箭头函数简写，返回一个数组字面量
    state: () => ({

    }),
    computed: {
        
    },
    actions: {

    }
})

export default useCityStore